import { Center, Loader } from "@mantine/core";
import React from "react";

const Loading = () => {
  return (
    <Center h={300}>
      <Loader size="xl" />
    </Center>
  );
};

export default Loading;
